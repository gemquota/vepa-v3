{
  "name": "vepa-v3",
  "version": "3.0.0",
  "description": "VEPA v3 — Vector Emergent Physics Automata (clean-slate recreation)",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "syntax-check": "find src -name '*.js' -exec node --check {} +"
  },
  "dependencies": {
    "pixi.js": "^8.18.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "vite": "^8.0.8",
    "vitest": "^3.2.7"
  }
}
